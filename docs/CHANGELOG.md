# Changelog

Все заметные изменения MH Calculator фиксируются в этом файле.

## Unreleased

### Changed

- На главной скрыта шапка `MH Calculator`, а ссылка с версией и списком изменений закреплена снизу и остаётся доступной при прокрутке навигации; футер отделён тонкой линией и более тёмным фоном.
- В расходах ресурсов экрана `Дерево навыков` каждая карточка показывает название ресурса сверху и центрированную строку `иконка : затраты` снизу.
- На главной странице кнопка `Ветки героев` приведена к `Дерево навыков`, чтобы её надпись совпадала с заголовком экрана `Дерево навыков`.
- На главной странице кнопка `build builder` приведена к `Builder`, чтобы совпадала с заголовком экрана билдера.

### Fixed

- Обновлён автотест `src/features/home/__tests__/HomeScreen.test.tsx`: добавлена проверка полного соответствия надписей всех кнопок главного экрана и ожидаемым заголовкам экранов, включая обновлённые названия.
- Обновлены контрактные документы: `docs/README.md` и `docs/divinity-talent-calculator-spec.md`.

## v1.7.1 - 2026-09-03

### Changed

- Из билдера удалён отменённый файловый JSON-сценарий и его скрытые действия; рабочие server draft, публикация и обновление сохранены в компактном блоке действий.
- Правила дерева билдера сведены к одному чистому контракту: ветки уникальны по колонкам, нижние ноды требуют предыдущие крупные навыки, а смена ветки атомарно очищает её данные и зависимый divinity-skill loadout.
- Auth, загрузка сущностей, validation, revision/server lifecycle и защита от устаревших ответов билдера вынесены из большого route-level screen в типизированный application-controller без изменения пользовательского workflow.
- Admin session, hero-build bootstrap/source selection, build theme и диагностические schemas перенесены из `shared` к явным feature-владельцам с публичными API; поведение auth, remote/fallback и UI не изменено.
- Каталог уровней божественности и generated fallback билдов перенесены к владельцам `game-data/divinity` и `builds/data`; bytes данных, runtime fallback и атомарный snapshot workflow сохранены.
- Удалён лишний file-based route `/admin/branch-builder.html`; единственной точкой входа билдера остаётся `/admin/branch-builder`, а exact route inventory и static-export проверки защищают контракт от повторного alias.
- Удалены недостижимые legacy-компоненты и общий request-helper без production consumers; правило конфликта create-публикации оставлено отдельной чистой моделью, а устаревший `@testing-library/jest-native` удалён в пользу встроенных matchers текущей React Native Testing Library.
- Архитектурные import-границы переведены с неполных regex-проверок на единый TypeScript import graph для alias, relative, re-export, type-only и dynamic imports; gate также отклоняет недостижимые production-модули и runtime-импорты test fixtures и добавлен в `npm run verify`.
- Продуктовые решения закреплены за capability specs, полный набор из 18 routes — за единственными владельцами в каталоге; `docs:check` теперь предотвращает потерю screen/spec, orphan-документы, устаревшие локальные ссылки и потерянные ссылки на правила.
- Транзитивное семейство Metro закреплено на совместимой с Expo SDK 56 версии `0.84.5`, которая удаляет уязвимый build-time parser изображений без смены React Native или публичного runtime-контракта.

### Fixed

- Повреждённый локальный прогресс или инвентарь `Божественности` больше не оставляет экран в бесконечной загрузке: пользователь может повторить чтение или явно сбросить только записи с ошибкой, не затрагивая исправные данные.

## v1.7.0 - 2026-09-02

### Added

- Добавлен трёхветочный калькулятор талантов божественности: независимый выбор начала и конца пути, точные цены каждой ноды из APK snapshot `1.48.0`, build `94`, и общий расход очков веры, Унаследованной божественности и Резонансных камней божественности.

### Changed

- Обновлён измеренный лимит production web-bundle с отдельным запасом под два маршрута, локальные данные и интерфейс нового калькулятора.
- Совместимые транзитивные зависимости Expo обновлены до patch-версий, закрывающих доступные production audit-исправления без смены Expo SDK.
- Калькулятор талантов, просмотр билдов и билдер используют общий игровой вид дерева: круглые оригинальные APK-иконки, синее, зелёное и фиолетовое свечение веток, ромбы соединений и постоянные компактные подписи нод. Калькулятор показывает только игровую шкалу уровней и учитывает три фактически расходуемых ресурса.
- Главная показывает одинаковые по ширине пункты навигации `Ветки героев` и `Билды героев`.

### Fixed

- Линии дерева больше не перекрывают игровые иконки и не меняют оттенок посередине: вместо четырёх рассчитываемых отрезков единая горизонтальная линия уровня проходит позади непрозрачных круглых контейнеров, неактивные изображения тонируются отдельным слоем без opacity, полностью яркими остаются соединения активных нод, а последнее свечение заканчивается у подписи выбранной ноды; номера строк `1–30` не обрезаются.
- У больших нод удалены уровни и длинные описания, у соединительных ромбов — видимые подписи. Выбор большого навыка оформлен единым ограниченным блоком, повторное нажатие заменяет навык без отдельного крестика, а список не накладывается на следующие уровни.
- Общая шапка внутренних экранов использует одинаковый заголовок `30px` максимум в две центрированные строки и геометрический шеврон, выровненный по вертикальному центру независимо от шрифта и длины текста.
- Карточка расходов веток показывает три ресурса вертикальными строками `иконка + название: количество`, поэтому длинные названия помещаются на мобильном экране.

### Documentation

- Корневой README обновлён по текущим возможностям приложения, получил заметную ссылку на GitHub Pages и актуальные команды локального запуска и проверок; релизные правила теперь требуют проверять README до тега и после публикации.
- Спецификация веток героев фиксирует включительный алгоритм выбора, формулы трёх ресурсов, контрольный пример, локальное сохранение и точную границу подсветки; встроенная инструкция приведена к тому же контракту.

## v1.6.1 - 2026-08-22

### Changed

- Под шапкой каталога `Билды героев` добавлена постоянная подсказка о том, что билды служат отправной точкой и требуют адаптации под конкретную ситуацию.
- Локальный fallback каталога обновлён из полного опубликованного набора: добавлены 20 отсутствовавших локально наборов билдов, а актуальные изменения билдов Бастет и Мораны перенесены из БД; всего без backend доступны 22 героя.
- Совместимые транзитивные зависимости Expo Metro обновлены до patch-версий с актуальными исправлениями безопасности без смены Expo SDK.

## v1.6.0 - 2026-08-20

### Added

- Добавлены три калькулятора еженедельного соперничества: `Вавилонская башня`, `Карта зодиака` и `Звериные эхо` с локальными игровыми наградами и иконками версии `1.48.0`, отдельным сохранением и общей встроенной инструкцией.
- Каждый экран рассчитывает 16 узлов соперничества и пять разделов по шесть заданий из общих исходных ресурсов; три собственных источника кешбэка и четвёртая общая галочка поддерживают изолированный или взаимный каскад без повторного применения награды.

### Changed

- Новые шкалы повторяют вид `Антиквариата`: четыре линии сундуков с прогрессом и конфигурационными крупными узлами; сводки обеих зон всегда показывают игровые иконки и числовые количества возможных наград, включая нули, без служебного дублирования.
- Все четыре галочки каждого события выключены по умолчанию и после сброса; основной ресурс и собственные сундуки используются в обеих зонах, а подписи явно называют источник кешбэка и правило `1 сундук = 1 ресурс`.

## v1.5.2 - 2026-08-17

### Fixed

- Маршруты `Призыв` больше не показывают системную шапку Expo Router; `Божественность`, `Антиквариат`, `Призыв`, `Билды героев` и Builder используют единую структуру, размер заголовка и поведение общей шапки.

## v1.5.1 - 2026-08-17

### Changed

- Экран `Призыв` точнее повторяет структуру антиквариата: короткое название перенесено в шапку, полный заголовок расположен под инструкцией, секция покупок переименована, а общий расход показан числом после иконки алмаза.

## v1.5.0 - 2026-08-17

### Added

- Добавлен калькулятор `Призыв`: имеющиеся и купленные ресурсы участвуют в каскадном расчёте очков, покупки меняются десятками с общими затратами алмазов сверху секции, а оригинальные игровые иконки, 16 узлов, награды, кешбэк, сохранение и встроенная инструкция доступны на новых маршрутах.

### Changed

- Зависимости Expo SDK 56 обновлены до совместимых patch-версий, требуемых релизной проверкой.

### Fixed

- Экран `Призыв` приведён к порядку блоков антиквариата: лишняя карточная сводка удалена, очки остались в компактной строке, а построчные суммы алмазов больше не дублируют общий итог секции обмена.
- В кешбэке антиквариата исправлены названия и добавлены игровые изображения фрагментов королевского и небесного сокровищ.

## v1.4.0 - 2026-08-13

### Changed

- Блокирующий loader билда героя теперь непрерывно остаётся видимым до завершения загрузки backend-данных и ограниченной предзагрузки критических иконок.
- Остальные иконки приложения получили локальный пиксельный loader «Тетрис-ряд»: быстрый cache-hit не мигает, заметная анимация завершается перед плавным появлением изображения, ошибка остаётся контролируемой заглушкой, reduced motion — статичным состоянием.

## v1.3.0 - 2026-08-13

### Added

- Для калькулятора «Антиквариат» добавлена встроенная инструкция: доступная кнопка `? Инструкция` открывает отдельный маршрут с правилами распределения монет, кешбэка, наград и сброса; инструкция «Божественности» сохраняет тот же общий паттерн.

### Fixed

- TypeScript-проверка больше не зависит от устаревшей локальной декларации маршрутов Expo после добавления новых экранов.

## v1.2.1 - 2026-08-13

### Changed

- Общий loader первоначальной загрузки обновлён: emoji-молния мягко пульсирует внутри расходящегося кольца, а две искры движутся по круговой орбите; reduced motion сохраняет статичное доступное состояние.
- Совместимые транзитивные зависимости обновлены в lockfile для устранения доступных audit-уязвимостей без смены Expo SDK или React Native.

## v1.2.0 - 2026-08-13

### Added

- Добавлены эксплуатационные gates: documented raw/gzip budget production web-entry, exact Chromium E2E для initial loader, remote/fallback каталога, build view, non-admin denial и dirty edit, а также общий безопасный recovery boundary.
- Добавлена ограниченная структурированная диагностика выбора fallback и отказа non-admin без raw ошибок, payload, stack или backend credentials.
- Добавлена единая ограниченная bootstrap-проверка Supabase с детерминированным manifest версий ресурсов; каталог и экран билда читают remote hero-build данные только после совместимого ответа и независимо переходят на bundled fallback при сбое своего ресурса.
- Добавлены детерминированные bundled snapshots билдов, проверенный last-known-good cache и автоматическое обновление snapshot через отдельную ветку и pull request.

### Changed

- Административный билдер разделён на независимо тестируемые editor, auth/status, server-command, async-currentness и validation-navigation модули; пользовательский workflow, revision-защита и гонки ответов сохранены.
- Локальная разработка, pull request CI и публикация Pages используют один закреплённый Node/npm toolchain и общий gate совместимости Expo, тестов, TypeScript и чистого web export; зависимости SDK 56 приведены к рекомендованным совместимым версиям.
- Источник билдов выбирается в порядке совместимый remote, самый новый полностью проверенный last-known-good, bundled snapshot; частичный, старый, несовместимый или повреждённый набор не заменяет принятые данные.
- Критические иконки каталога и выбранного билда теперь предзагружаются ограниченным набором, ожидают свободный слот вместо ложной готовности при заполненном реестре и используют общий кешируемый контейнер с фиксированным placeholder/error-состоянием без скачка размеров.
- Первоначальная загрузка каталога, билда и административного экрана использует единый доступный loader с поддержкой reduced motion; фоновые обновления не скрывают уже показанный контент.
- Серверный билд героя теперь имеет ровно одно состояние: черновик атомарно становится опубликованным, а опубликованный билд нельзя удалить или вернуть в черновик.
- Действия удаления опубликованного билда убраны из экрана героя и административного билдера.
- Прямые записи в `hero_build_sets` закрыты; создание черновика, публикация и обновление опубликованного билда выполняются отдельными RPC с обязательной server-проверкой admin claim и исходного состояния.
- Режим редактирования опубликованного билда показывает единственное действие `Обновить` только при реальных изменениях, проверяет и отправляет полный комплект без создания черновика и скрывает действие после успешного обновления.
- Локальные изменения опубликованного билда сохраняются при переключении вкладок и ошибках обновления; уход назад, выход, смена героя и закрытие страницы теперь требуют подтверждения.
- Supabase payload билдов теперь читается как недоверенный JSON и проходит версионную доменную валидацию; невалидный remote не заменяет локальный fallback и возвращает диагностируемую причину.
- Сохранение и публикация билдов теперь используют оптимистичную проверку revision; параллельное изменение показывает отдельный конфликт и не сбрасывает локальные правки.
- Для билдов ведётся неизменяемая история авторских revisions; опубликованный снимок можно восстановить только созданием новой опубликованной revision.

### Fixed

- Configured web export гарантированно встраивает публичные Supabase URL и anon key через прямую Expo compile-time boundary; чистая сборка больше не выбирает локальный fallback из-за потерянного env.
- Production static export с настроенным Supabase больше не создаёт browser auth storage во время Node SSR; публичные env значения при этом встраиваются в browser bundle для последующей hydration.
- SSR и первый hydration-render каталога теперь начинают с одинакового loader-only состояния; fallback или remote-контент выбирается только после client effect без несовпадающей разметки.
- Возврат из общего recovery boundary восстанавливает главную даже при сбое уже на маршруте `/`; диагностический лог строится только из явного allowlist, а deployment artifact проверяется budget после export с реальными env.
- Последовательные клики по цвету пробуждения оружия больше не теряются при объединённом React update; async-переходы билдера используют единый атомарный controller без параллельных request-latches экрана.
- Каждое LKG-поколение теперь хранится одним атомарным AsyncStorage blob; все storage-операции имеют deadline, поэтому зависшее чтение выбирает bundled fallback, зависшая запись не блокирует проверенный remote и не ломает очередь следующих сохранений.
- Автоочистка LKG больше не удаляет повреждённые или недоступные generation keys без CAS; до `128` exact кандидатов проверяются, а очистка затрагивает только валидные поколения за четырьмя наиболее свежими.
- Каталог и detail теперь принимают remote только из одного полного snapshot; LKG восстанавливается из ограниченного набора immutable-поколений при повреждённом или проигравшем гонку pointer, bundled-файлы проверяются без реконструкции manifest, а SQL/workflow используют согласованный byte budget, exact branch lease и только открытый PR.
- Fresh Supabase migration chain теперь явно выдаёт `anon, authenticated` право `SELECT` на билды героев; published-only RLS продолжает скрывать черновики, а права прямой записи не открываются.
- Bootstrap manifest теперь атомарно хеширует полный ordered snapshot опубликованных метаданных одним `SECURITY INVOKER` SQL RPC через anon/RLS, не зависит от Data API pagination, строго ограничивает body по UTF-8 байтам и управляет реальными source-переходами каталога и экрана билда.
- Отмена загрузки каталога теперь немедленно завершает wrapper типизированным исходом без зависших closures или ложного fallback, а локальный Hero Build симметрично диагностирует `not-configured` ровно один раз на героя.
- Смена героя в маршруте просмотра теперь атомарно сбрасывает прежний билд и выбирает валидную вкладку вместе с новым ответом без старого контента или промежуточной пустой карточки.
- Восстановление admin-сессии в режиме редактирования больше не показывает editor до начала загрузки опубликованного билда.
- Зависший запрос каталога через `8` секунд переходит к локальному fallback и освобождает timer; событие reduced motion больше не перезаписывается устаревшим системным snapshot.
- Каталог билдов больше не показывает bundled Бастет и Морану перед выбором источника: remote-режим содержит только опубликованные backend IDs, а ошибка явно включает локальный fallback с повторной попыткой.
- После выхода из редактора опубликованного билда старая dirty-форма больше не возвращается при повторном входе; подтверждения переходов сериализованы и не продолжаются после закрытия экрана.
- Ошибка выхода из редактора больше не очищает server revision: сохранённую dirty-форму можно сразу обновить, а поздний ответ отменённого запроса не подменяет её revision.

- Полная валидация опубликованного билда теперь открывает первую проблемную вкладку, показывает ошибку у соответствующего поля и прокручивает к его секции.
- Неуспешная загрузка другого героя больше не меняет identity сохранённой формы и не оставляет последующее сохранение прежнего героя в бесконечном pending.
- Восстановление версии теперь требует историю того же героя, а все операции версий отклоняют запрос без идентификатора авторизованного администратора.
- Поздний ответ сохранения или публикации предыдущего героя больше не меняет состояние и сообщения после выбора другого героя.
- Административный билдер, черновики и изменение билдов теперь доступны только пользователям с Supabase admin claim; RLS оставляет остальным только чтение опубликованных билдов.
- Ошибка локального выхода после non-admin login больше не подменяет отказ `Недостаточно прав администратора.`.
- Runtime-проверка Supabase-билдов теперь отклоняет неполные leaf-вкладки, отсутствующий у них `gameMode`, лишние/accessor/symbol-поля, sparse/слишком глубокие или объёмные структуры и неканонические даты; непустая backend-строка без собственного `payload` считается повреждённой, проверки строк, ключей и divinity loadout ограничены budgets, а fallback сохраняет типизированную причину `invalid-data`.

### Documentation

- Правила выпуска описывают воспроизводимую установку и единый verification gate, production audit с классификацией цепочек и запрет слепого `npm audit fix --force`.
- Постоянные specs приведены к фактическим именам и формам типов export билдера, каталога мажорных скиллов, прогресса божественности и нормализованного ввода антиквариата.

## v1.1.0 - 2026-08-12

### Added

- На главном экране добавлен footer с текущей версией приложения и ссылкой «Что нового» на последний GitHub Release.
- Билдер поддерживает серверные черновики вкладок: отдельные списки героев, восстановление незавершённого комплекта и удаление черновика после публикации.

### Fixed

- Проверка иконок антиквариата приведена в соответствие с каталогом: записи с PNG проверяют изображение, записи без PNG — контролируемую заглушку.

### Documentation

- Документация объединена в четыре постоянные спецификации функций приложения.
- Добавлены каталог возможностей, правила ведения changelog, тегов и GitHub Releases.
- Временные материалы `docs/superpowers` исключены из Git.

## v1.0.0 - 2026-08-12

### Added

- Калькулятор божественности с диапазоном уровней, ручным прогрессом, автозаполнением и учётом собственных ресурсов.
- Каталог и просмотр билдов героев с фильтрами, вкладками, экипировкой, пробуждением оружия и божественностью.
- Административный билдер с вкладками, валидацией, JSON-экспортом и Supabase-публикацией.
- Калькулятор события «Антиквариат» с сохранением ввода, каскадными наградами, распределением монет и сундуками соперничества.
